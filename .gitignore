.venv
pretrained_models
__pycache__
.cache
outputs
studio_data

# Frontend build deps/artifacts
node_modules
*.tsbuildinfo

# Local auth secret (never commit to the public repo)
token.md

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
